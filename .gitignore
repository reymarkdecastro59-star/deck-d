# Dependencies
web/node_modules/

# Build output
web/dist/

# Environment variables
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
*.local

# Claude (settings + memory — not for source control)
.claude/

# Planning docs (commit when ready)
docs/

# Figma binary (large, not needed in source control)
figma/*.fig
